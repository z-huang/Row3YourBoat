const MySlackCount = ({ count, url }) => (
  <div className="text-xl font-bold text-center">
    你今天划水了 {count} 次 🐟，不要再看 {url} 網頁了!
  </div>
);

export default MySlackCount;
